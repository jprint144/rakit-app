import { PlaceholderPage } from "@/components/placeholder-page";

export default function IdeaPage() {
  return (
    <PlaceholderPage
      title="Idea"
      description="Idea vault — simpan ide dalam bentuk teks, dokumen, gambar, atau link."
    />
  );
}
