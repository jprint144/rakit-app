import { PlaceholderPage } from "@/components/placeholder-page";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Folder utama, identitas agency, kategori custom, tema, backup data."
    />
  );
}
